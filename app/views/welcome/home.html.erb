<div class="row">
    <div class="col-sm-12">
        <h1>Welcome to School Uniform Resale!</h1>
    </div>
</div>
<div class="row">
  <div class="col-sm-8 col-md-4">
    <div class="well">
      <h3>My Schools</h3>
        <% current_user.schools.each do |school| %>
        <div>
            <%= link_to school.name, school %><br/>
            <%= school.formatted_address.html_safe %>
            <%= "#{school.email}" unless school.email.blank? %>
            <!--
            < % = "#{school.phone}<br/>" unless school.phone.blank? % > -->
            <%= link_to school.url unless school.url.blank? %>
        </div>
        <% end %>
      </table>
    </div>
  </div>

  <div class=" col-sm-8 col-md-8">
    <h3>Buy &amp; Sell</h3>
    <%= link_to "List Clothing for Sale", new_item_path %><br/>
    <%= link_to "Browse Clothing for Sale", items_path %><br/>
    <hr/>
    <p>
    <%= link_to "Items I am Selling (#{current_user.items.length})", my_items_path %>
    <br/>
    <%= link_to "Items I am Watching (0)", items_path %>

    </p>

    <hr/>
    <h3>Messages</h3>
    <p>
    <%= link_to "Inbox : #{current_user.incoming_messages.length} / #{current_user.unread_messages.length}", messages_path, id: "inbox" %>
    </p>

    <h3>Friends (<%= current_user.friends.length %>)</h3>
    <% if current_user.friends.length == 0 %>
    <p>
      It looks like you don't have any School Uniform Friends yet. Why not invite someone to join now?
    </p>
    <% else %>
        <% current_user.top_friends.each do |f| %>
            <div class="row">
                <div class="col-sm-12">
                    <strong><%= link_to f.name, f %></strong>
                </div>
            </div>
        <% end %>
        <%= link_to "See all....", friends_path %>
    <% end %>
    <div>
    <%= link_to "Invite a friend", invite_path %>
    </div>

  </div>
</div>
